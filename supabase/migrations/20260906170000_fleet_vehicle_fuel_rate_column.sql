-- เฟส 1/2: เพิ่มคอลัมน์ "อัตราสิ้นเปลืองที่ทางราชการกำหนด" (กม./ลิตร) รายคัน
--
-- ทำไมไม่ใช้ efficiency_min/efficiency_max ที่มีอยู่แล้ว:
--   สองคอลัมน์นั้นคือ "ช่วงที่เป็นไปได้ทางวิศวกรรม" ใช้ตั้งธงว่ากรอกตัวเลขผิดหลัก
--   (ดู 20260830200000_fleet_vehicle_efficiency_range_override.sql) ไม่ใช่ตัวเลขที่
--   หน่วยงานประกาศไว้ว่ารถคันนี้ต้องวิ่งได้กี่ กม./ลิตร ซึ่งเป็นค่าเชิงบริหาร/ระเบียบ
--   ที่ต้องพิมพ์ลงช่อง "อัตราที่กำหนด" ในบันทึกข้อความรายงานการใช้น้ำมันเชื้อเพลิงรายเดือน
--   เอาสองเรื่องนี้มาปนกันแล้วจะพังทั้งคู่: ขยับเกณฑ์จับค่าผิดปกติทีเดียว เอกสารที่ส่ง
--   ผู้บริหารก็เปลี่ยนตาม และกลับกันคือแก้ตัวเลขในเอกสารแล้วธงเตือนหายเงียบๆ
--
-- ปล่อยว่างได้ (NULL) = ยังไม่ได้กำหนด เอกสารจะพิมพ์ช่องนั้นเป็นขีดว่างให้เขียนมือ
-- ไม่ใช่เดาค่าให้ เพราะเป็นตัวเลขที่ใช้ประกอบการตรวจสอบการเบิกจ่าย

ALTER TABLE public.fleet_vehicles
  ADD COLUMN IF NOT EXISTS fuel_rate_standard_kml numeric(6,2);
