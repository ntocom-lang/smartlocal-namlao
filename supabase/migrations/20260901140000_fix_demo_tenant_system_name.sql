-- ล้าง system_name ที่สนามซ้อม (slug='demo') ยืมชื่อของ อปท. จริงมาแสดง
--
-- ที่มา: E2E 2026-08-30 พบว่าหน้า /auth ของ demo.rk-networks.com ขึ้นข้อความ
-- "เข้าสู่ระบบเทศบาลตำบลน้ำเลา" ทั้งที่ header/footer แสดง "เทศบาลตำบลสาธิต" ถูกต้อง
--
-- สาเหตุ: 20260829113107_seed_demo_tenant.sql คัดลอก m.system_name จากน้ำเลามาให้ demo ด้วย
-- (ตั้งใจคัดลอกเฉพาะ "ค่าตั้งค่า" แต่ system_name เป็นชื่อเฉพาะของหน่วยงาน ไม่ใช่ค่าตั้งค่ากลาง)
-- ส่วนหน้าที่แสดงชื่อระบบเลือก system_name ก่อน name เสมอ เช่น src/pages/AuthPage.jsx:309
--   `เข้าสู่ระบบ${tenant?.system_name || `${tenant?.name} One Data`}`
--
-- ⚠️ ประเด็นที่ต้องแก้จริง ไม่ใช่แค่ความสวยงาม: ผู้ทดสอบ/ผู้ใช้ที่เห็นชื่อ อปท. จริงบนสนามซ้อม
-- อาจเข้าใจว่ากำลังกรอกข้อมูลเข้าระบบของ อปท. นั้น
--
-- ทางแก้: ตั้งเป็น NULL ให้ตกไปใช้ fallback `${name} One Data` ของหน้าเว็บ
-- → "เทศบาลตำบลสาธิต One Data" ซึ่งเปลี่ยนตามชื่อ อปท. เองอัตโนมัติ ไม่ต้อง hardcode ซ้ำอีกที่
--
-- รันซ้ำได้ และจำกัดขอบเขตที่ slug='demo' เท่านั้น ห้ามแตะ อปท. อื่นเด็ดขาด

BEGIN;

UPDATE public.municipalities
SET system_name = NULL
WHERE slug = 'demo'
  AND system_name IS NOT NULL;

COMMIT;
