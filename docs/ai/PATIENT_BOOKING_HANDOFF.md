# Patient transport booking — implementation handoff (2026-09-18)

สถานะ ณ 2026-09-18: ผู้ใช้สั่ง deploy แล้ว โค้ด commit/push ในสาขา codex/patient-transport-booking-20260918, PR #211; apply migrations ครบ 5 ชุดแล้ว กำลัง release ผ่าน CI ทุก tenant ยังปิดรับจองใหม่ (0 settings ที่ enabled, 0 booking records)

## ขอบเขตที่ทำ

- `/patient-transport`: หน้าเด่น จอง 3 ขั้น ญาติจองแทน ติดตามสถานะ และแจ้งพร้อมรับกลับ ไม่มีเลขสมาชิกกองทุน
- อบต. เป็นผู้ประสานงานตามบัญชีที่ผู้ดูแลกำหนด คนขับมีบัญชีแยก กองทุนไม่ต้องเพิ่มคนใช้ระบบ
- หน้าแรก ServiceHub ชูบริการเมื่อหน่วยงานเปิดใช้แล้ว; ทางเข้าจากหน้าเจ้าหน้าที่และทะเบียนหน่วยงานเจ้าของรถ
- คำขอใหม่แยกจาก `document_requests` และ referral workflow เดิม ประวัติ/เอกสารเดิมคงอยู่ เมื่อเปิด booking mode ลิงก์เดิมเปลี่ยนเข้าจองใหม่ และ trigger กันเบราว์เซอร์เก่าสร้างคิวคู่ขนาน
- ระบบเสนอรวมเที่ยวจากวัน เส้นทาง ความสมัครใจ เวลาและความจุ คนตรวจแผนจาก server ก่อนยืนยัน
- PostgreSQL คำนวณทั้งช่วงรอ/ขากลับและรถวิ่งเปล่า ล็อก settings row ของรถคันเดียวตลอดธุรกรรมเพื่อไม่ให้ยืนยันคิวชนกัน; เที่ยวที่จบก่อนแผนยังกันเวลาตามแผนเดิมแบบ conservative
- ยืนยันเที่ยวตรวจรุ่น settings และทุกรายการจองเทียบแผนที่ตรวจแล้ว ถ้ามีการแก้ pickup แม้เวลาเดิมก็ต้องตรวจใหม่
- คนขับบันทึกรับ/ส่งรายผู้เดินทาง ไม่จบเที่ยวขณะยังส่งคนไม่ครบ ใช้ operation UUID + expected revision กันเน็ตหลุดยิงซ้ำแล้วข้ามขั้น
- มีแจ้งเหตุขัดข้อง ประสานแก้ไข ขอเลิกก่อน/หลังยืนยัน คืนคิวก่อนรถออก และนำรายบุคคลออกหลังประสานแผนดูแลต่อ (ไม่อนุญาตระหว่างอยู่บนรถ)
- เจ้าหน้าที่แก้รายละเอียดคำขอที่ยังไม่ยืนยัน หลังประสานผู้จอง พร้อมเก็บ before/after และเหตุผล ไม่ต้องยกเลิก/กรอกใหม่เพื่อเติมเวลาขากลับ
- ประวัติ audit และแจ้งเตือนในแอปเขียนใน transaction เดียวกัน รายงานเที่ยวปิด 30 วันล่าสุดและงานค้างคำนวณจากข้อมูลจริง ไม่มี LINE/SMS/email/GPS หรือบริการเสียเงินใหม่

## ขอบเขตสิทธิ์

ตารางใหม่เปิด RLS และถอน direct grants จาก PUBLIC/anon/authenticated ตั้งแต่ migration แรก อ่าน/เขียนผ่าน RPC ที่ตรวจ tenant + actor เท่านั้น helper functions ถูก revoke ไม่เปิดให้ client เรียก มี public RPC เพียงข้อมูลบริการ ไม่มีรายชื่อผู้ป่วย

ผู้จองเห็นของตัวเอง รวมที่ยื่นแทน; คนขับเห็นข้อมูลติดต่อและการช่วยเหลือเฉพาะผู้เดินทางที่ยังอยู่ในเที่ยวที่มอบหมาย ไม่มี consent snapshot หรือรายการรอจัดคิว ผู้ประสานงานเห็นทั้งคิวเฉพาะ tenant ของตน ผู้ดูแลตั้งค่าทะเบียนได้ ไม่มีการตัดสิทธิ ลงนาม อนุมัติค่าใช้จ่าย หรืออนุมัติแทนกองทุนอัตโนมัติ

อ้างอิงแนวทางเทคนิค: [Supabase database functions](https://supabase.com/docs/guides/database/functions), [PostgreSQL explicit locking](https://www.postgresql.org/docs/17/explicit-locking.html). การใช้ SECURITY DEFINER ต้องจำกัด search_path และ EXECUTE privileges พร้อมตรวจสิทธิ์เองทุกจุด ไม่มี server key ฝังใน frontend

## เปิดใช้เป็นรายหน่วยงานเท่านั้น

หลังอนุมัติการเปลี่ยนฐานข้อมูล ต้อง apply ตามลำดับ:

1. `20260918110000_patient_booking_tables.sql`
2. `20260918110100_patient_booking_rules.sql`
3. `20260918110200_patient_booking_api.sql`
4. `20260918110300_patient_booking_amend.sql`
5. `20260918113759_patient_booking_calendar.sql` (สร้างชื่อด้วย Supabase CLI)

ไม่ใช้ bulk db push โดยไม่ตรวจ migrations อื่น ไม่แทรก seed เปิดบริการให้ tenant จริง Default ไม่มี settings row และยังปิดรับจองใหม่

ข้อมูลที่ต้องตรวจรับในหน้าตั้งค่าก่อน enabled=true: เจ้าของรถจากทะเบียน, คนขับและผู้ประสานงานแยกบัญชี, ความจุที่นั่ง/รถเข็น/เปล, เวลาบริการ, รายชื่อเส้นทางและเวลาเดินทาง, ปฏิทินวันหยุดครอบคลุมวันจอง, เบอร์ติดต่อ, ขอบเขตมอบหมายและข้อความการใช้ข้อมูลที่ตรวจรับแล้ว

ค่า 08:30–16:30, ระยะเผื่อ 15 นาที และขึ้นลง 15 นาทีเป็นค่าร่าง ไม่ใช่การยืนยันนโยบายหน่วยงาน ไม่มีการเดาความจุรถ ต้องระบุผู้ช่วยเคลื่อนย้ายจริงรายเที่ยวรถเข็น/เปล รุ่นนี้รวมเที่ยวอัตโนมัติเฉพาะผู้เดินได้ ไม่สมมติว่าการติดตั้งที่นั่ง รถเข็น และเปลใช้ได้พร้อมกัน

เจ้าหน้าที่ต้องดูแลเมื่อคนขับลา/รถเสีย/เวลารักษายืดเยื้อและประสานกรณีออกนอกเวลาราชการ ไม่ต้องยืนยันความพร้อมรายวัน การตรวจดูหน้าแอปอัปเดตทุก 60 วินาทีเมื่อเปิดอยู่และเมื่อกลับมาที่หน้า ไม่ใช่ push notification ขณะปิดแอป หากติดต่อผ่านระบบไม่ได้ยังต้องโทรประสาน

## ผลทดสอบ

- Scoped ESLint ผ่านในไฟล์ฟีเจอร์และจุดเชื่อมที่ตรวจ
- `vite build` ผ่าน ใช้ URL/key ปลอมไป loopback ไม่มีการอ่านหรือเปลี่ยน `.env` build นี้ใช้สำหรับตรวจ compile เท่านั้น ห้าม deploy artifacts ชุดนี้
- PostgreSQL ในเครื่องด้วย PGlite 0.5.8 (Apache-2.0 ติดตั้งแยกนอก repo): migrations execute ได้ ตรวจสิทธิ์ตรงทุกบทบาท/ข้าม tenant, public projection, direct table/helper denial, consent/ตัวแทน, duplicate, idempotency ownership, เวลารถชน, ความจุ, stale/tampered plan, revision, รับส่งรายคน, ขากลับ, ยกเลิก, incident, amend และ legacy intake guard
- React + Playwright + Edge เชื่อม RPC กับ PostgreSQL จำลองจริงในเครื่อง ผ่านจอง → อบต.ยืนยัน → คนขับรับส่งรายคน → ผู้จองเห็นจบเที่ยว ไม่มีการ mock ผลสำเร็จของการจอง
- ตรวจ rendered UI 320/390/768/1024px ในมุมมองประชาชน ผู้ประสานงาน คนขับ และผู้ดูแล ไม่พบ horizontal overflow; ตรวจภาพหน้าแรก/คิวที่ 390px หลังแก้พื้นหลังแล้ว
- Browser harness block non-loopback requests ไม่มีข้อมูลประชาชนจริง ไม่มีการส่งข้อความหรือเขียน production
- PGlite เป็น single connection จึงยังไม่ใช่หลักฐาน race test จาก PostgreSQL สอง connection จริง ต้องทดสอบพร้อมกันสองบัญชีบน Demo หลังอนุมัติ apply

คำสั่งทดสอบ (ใช้โมดูล PostgreSQL ในเครื่องเท่านั้น):

```powershell
$env:PATIENT_PGLITE_MODULE = 'D:/tmp/patient-booking-dbtest-20260918/node_modules/@electric-sql/pglite/dist/index.js'
node tests/patient-booking-db.test.mjs
node tests/patient-booking-browser.test.mjs
```

ไม่มี production dependency ใหม่ ทดสอบใช้ dependency เดิมของ repo และ PGlite ที่ติดตั้งเฉพาะที่ทดสอบ ค่า license/service เพิ่ม 0 บาท ยังคงมีแรงงานตรวจรับ/ดูแลและต้นทุนการเดินรถจริง

## Release gates ที่ยังค้าง

1. แก้ missing `node:process` import ที่ vite.config.js ในรอบ release แล้ว `node scripts/lint-blocking.mjs` ผ่านหลังรวม master ล่าสุด
2. ตรวจ dependency columns กับ schema จริงและ apply ครบแล้ว; ก่อนเปิดรับจองยังต้องทดสอบ Demo ด้วยบัญชีแต่ละบทบาท รวม two-connection race และเน็ตมือถือขาดกลางคำสั่ง
3. ทดสอบจอและการใช้งานบนโทรศัพท์จริงของคนขับ; viewport simulation ไม่ใช่ผลรับรองอุปกรณ์จริง
4. ผู้รับผิดชอบตรวจขอบเขตมอบหมาย/การใช้ข้อมูล/อายุเก็บและกระบวนการลบข้อมูลกับฉบับปัจจุบันก่อนเปิดรับข้อมูลสุขภาพ ไม่มีการสร้าง cron ลบข้อมูลหรือเดาระยะ retention อัตโนมัติในงานนี้
5. ผู้ใช้สั่ง deploy ในบทสนทนาแล้ว กำลังดำเนิน release ตามคำสั่งนั้น; การเปิดรับข้อมูลจริงยังต้องตั้งค่าและตรวจรับข้างต้น

## ข้อจำกัดเมื่อขยาย

รองรับหนึ่งรถ/หนึ่งคนขับต่อ tenant ตามโจทย์ การล็อกเป็นระดับ tenant จงใจ serialize งานของทรัพยากรเดียว ไม่ใช่ fleet optimizer ไม่ใช้ live traffic/GPS และไม่เปลี่ยนเส้นทางยืนยันแล้วเอง ข้อมูลปิดงานแสดง 30 วันล่าสุด งานเปิดดึงสูงสุด 1,000 รายการพร้อมป้ายเตือนเมื่อถึงเพดาน audit/notices ยังเติบโตตามจำนวนคำสั่ง ต้องกำหนด retention/archival ก่อนใช้ปริมาณมากหรือขยายหลายรถ

## เพิ่มปฏิทินและตารางสำหรับประชาชน (อนุมัติในบทสนทนา 2026-09-18)

- ปุ่ม “ดูตารางรถ” และ CTA หน้าแรก สลับ “ปฏิทิน / ตาราง” และจำมุมมองใน localStorage ไม่มีข้อมูลผู้ป่วยอยู่ใน storage นี้
- ดูได้ก่อนเข้าสู่ระบบ กรองเดือน ช่วงวันที่ เส้นทาง และเฉพาะเที่ยวที่ร่วมได้ บนมือถือแสดงตารางเป็นรายการเรียงวัน ไม่ต้องเลื่อนแนวนอน; ตัวกรองละเอียดพับเก็บได้
- Public RPC ส่งเฉพาะช่วงรถถูกใช้ ช่วงว่าง สถานะ และข้อมูลรวมของเที่ยวร่วม ไม่มีชื่อ เบอร์ จุดรับบ้าน รายชื่อสมาชิกเที่ยว หรือข้อมูลการช่วยเหลือรายคน เที่ยวที่ไม่เปิดร่วมซ่อนปลายทางและจำนวนคนด้วย
- จำนวนคนรวมผู้ติดตาม ที่นั่งเหลือคิดจากผู้ที่ยังอยู่ในเที่ยว คำขอรอยืนยันไม่กันที่นั่ง แสดงตามแผนจริงและโหลดใหม่ทุก 60 วินาทีเมื่อหน้าเปิดอยู่/กลับมา focus
- วันหยุด วันงดบริการ วันที่ปฏิทินยังไม่ตรวจครบ และช่วงที่พ้นกำหนดจอง ไม่มีปุ่มขอจองวันว่าง เหตุขัดข้องทำให้วันนั้นไม่แสดงช่วงว่าง
- การขอร่วมเที่ยวเติมวัน เส้นทาง รูปแบบขากลับ และเวลารับกลับ ผู้จองต้องระบุเวลานัดจริงเอง ระบบตรวจความจุ/เวลา/จุดรับในพื้นที่ก่อนรับคำขอ ไม่มีการตีความเวลาออกเป็นเวลานัด
- `requested_trip_id` ผูกคำขอกับเที่ยวเดิม เจ้าหน้าที่ตรวจแผนรวมและประสานเวลาที่เปลี่ยนก่อนยืนยัน `confirm_join` ปรับเที่ยวเดิมภายใต้ resource lock พร้อม revision และ operation UUID ไม่สร้างรถเที่ยวใหม่ซ้อน
- เพิ่มคนได้เฉพาะเที่ยวที่ยังไม่ออก มีผู้เดินทางเดิมที่ยินดีร่วมเที่ยวและเดินได้เองทั้งหมด คนขับต้องตรงค่าปัจจุบัน และไม่มีคำขอยกเลิกค้าง หากเที่ยวเดิมปิด/เปลี่ยนจนร่วมไม่ได้ให้เจ้าหน้าที่ประสานผู้จอง ใช้ยกเลิกและยื่นใหม่เมื่อต้องเปลี่ยนเป็นอีกเที่ยว
- เมื่อยืนยันเพิ่มคน อาจต้องเริ่มรับเร็วขึ้นเพื่อเผื่อจุดรับ ระบบแจ้งในแอปให้ผู้เดินทางเดิม ผู้ขอร่วม และคนขับตรวจแผนล่าสุด ต้องโทรประสานเพิ่มเติมหากเจ้าตัวไม่ได้เปิดแอป
- ช่วงว่างเป็นช่วงตามแผน ไม่รับรองว่าจะพอสำหรับเส้นทางที่เลือก และเวลาเดินทางเป็นค่าที่ตั้งไว้ ไม่ใช่ข้อมูลจราจรสด

ตรวจเพิ่มผ่าน: PostgreSQL ตรวจ public projection, วันหยุด, free intervals ของขากลับภายหลัง, ช่วงวันที่เกินกำหนด, request rollback เมื่อร่วมไม่ได้, การแข่งขันแย่งที่นั่งด้วยแผนเก่า, ยืนยันเข้าเที่ยวเดิมและ retry; Browser ทดสอบผู้ไม่เข้าสู่ระบบ, จำมุมมอง, ขอร่วมจากตารางจนเจ้าหน้าที่รับเข้าเที่ยวเดิมและเต็ม, สองมุมมองที่ 320/390/768/1024px ไม่มี overflow. PGlite ยังเป็น single connection ไม่แทน two-connection race test บน Demo

ค่า license/service เพิ่ม 0 บาท ไม่มี dependency production ใหม่ เพิ่มดัชนีตาม tenant/วันและ trip_id เพื่อลดการอ่านประวัติซ้ำ จำกัด public query ครั้งละ 62 วันและล่วงหน้า 180 วัน การเปิดเผยแม้เป็นจำนวนรวมกับเส้นทางอาจทำให้คาดเดาตัวบุคคลในชุมชนเล็กได้ ผู้รับผิดชอบต้องตรวจข้อความแจ้งการใช้ข้อมูลและนโยบายเผยแพร่ตารางก่อนเปิดจริง ห้ามใส่ชื่อ/ที่อยู่ผู้ป่วยในชื่อเส้นทาง


## ผลตรวจฐานข้อมูลหลังติดตั้ง

RLS เปิดครบ 6 ตาราง, anon/authenticated ไม่มี direct table grants และเรียก private planning helper ไม่ได้ Public calendar ของ Demo ตอบ enabled=false และ days=[] ตามค่าตั้งต้น Security advisors เพิ่ม INFO เรื่อง RLS ไม่มี policy 6 ตาราง (จงใจใช้ RPC-only) และ WARN เรื่อง SECURITY DEFINER callable: anonymous 2 endpoints / authenticated 12 endpoints ซึ่งจำกัด grants, search_path และตรวจ tenant/role ในฟังก์ชันแล้ว ไม่มี ERROR ใหม่ แต่ไม่ได้อ้างว่าทั้งโปรเจกต์ไม่มี advisory เดิม

## ด่านกันคำขอที่ยืนยันไม่ได้ ตั้งแต่หน้าฟอร์ม (2026-09-18, ต่อจาก #211)

เดิมคำขอที่ระบบยืนยันไม่ได้อยู่แล้ว (วันหยุด เสาร์–อาทิตย์ วันที่ยังไม่ตรวจปฏิทิน พ้นกำหนด
จองล่วงหน้า รถไม่พร้อม เวลานัดที่รถต้องออกก่อนเวลาบริการ) ยังเข้าคิวได้ แล้วไปตายตอน
เจ้าหน้าที่กด "ตรวจแผน" ⇒ ต้องโทรประสานกลับทุกใบ รอบนี้ย้ายด่านมาไว้ก่อนส่ง

- `patient_booking_info` ส่ง `buffer_minutes`/`boarding_minutes` เพิ่ม เพื่อให้ฟอร์มคำนวณ
  ช่วงเวลาที่รถต้องออกและกลับถึงได้เอง (สูตรเดียวกับ `ptb_plan` กรณีผู้เดินทางรายเดียว)
  ตัวเลขนี้เป็นค่าตั้งของหน่วยงาน ไม่ใช่ข้อมูลส่วนบุคคล
- ฟอร์มจองเรียก `patient_booking_calendar` ของวันที่เลือก แล้วบล็อกพร้อมบอกเหตุผลตาม
  `day_status` เดิมของปฏิทิน (`closed`/`unverified`/`lead_time`/`unavailable`/`issue`/`past`)
  `min`/`max` ของช่องวันที่ไล่ตาม `min_lead_days` และเพดาน 180 วัน
- เจ้าหน้าที่ที่รับเรื่องแทน (`staffEntry`) เห็นคำเตือนสีเหลืองแต่ยังส่งได้ เพราะเคสประสาน
  พิเศษต้องทำได้ ฝั่ง server ก็ยกเว้นให้ `admin`/`coordinator` ด้วยเงื่อนไขเดียวกัน
- `patient_booking_submit` ตรวจซ้ำฝั่ง server (วันหยุด ปฏิทินยังไม่ตรวจ และ `unavailable`)
  กันเบราว์เซอร์ที่เปิดค้างและการเรียก RPC ตรง ส่วน `issue` ของวันนั้นยังเป็นคำเตือนฝั่ง
  หน้าจออย่างเดียว เพราะเหตุขัดข้องอาจคลี่คลายก่อนถึงวันเดินทาง
- ดีฟอลต์ช่อง "ผู้ติดตาม" เปลี่ยนจาก 1 เป็น 0 (เจ้าของระบบเลือก 2026-09-18) คนที่ไปคน
  เดียวแล้วไม่แก้ค่าจะไม่กันที่นั่งเกินจริงอีก ทำให้รวมเที่ยวได้มากขึ้น
- หน้าตั้งค่าชี้ทางไปแก้ `min_lead_days` ที่ทะเบียนหน่วยงานรับเรื่องต่อ (เมนู "ประเภทคำขอ
  เอกสาร") และแสดงค่าปัจจุบันของเจ้าของรถที่เลือกไว้ ค่านี้ไม่ได้อยู่ในตารางตั้งค่าโมดูลนี้

Migration: `20260918170000_patient_booking_day_guards.sql` (CREATE OR REPLACE 3 ฟังก์ชัน
ยกมาทั้งตัวจากของเดิมแล้วแก้เฉพาะจุด ไม่มี placeholder) ถ้ายังไม่ apply หน้าเว็บจะไม่พัง
แต่จะไม่เตือนเรื่องเวลานัดนอกเวลาบริการ เพราะ `journeyWindow()` คืน null เมื่อไม่มีค่าเวลา

ทดสอบ: `node tests/patient-booking-db.test.mjs` เพิ่มชุดวันหยุด/ปฏิทินยังไม่ตรวจ/รถไม่พร้อม
และยืนยันว่าเจ้าหน้าที่ยังรับเรื่องแทนได้ · `node tests/patient-booking-browser.test.mjs`
เพิ่มเคสประชาชนเลือกวันเสาร์และเวลานัด 08:45 แล้วปุ่ม "ต่อไป" ถูกปิดพร้อมเหตุผล
